#!/usr/bin/env node
"use strict";
var _fs = _interopRequireDefault(require("fs"));
var _path = _interopRequireDefault(require("path"));
var _ora = _interopRequireDefault(require("ora"));
function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
var spinner = (0, _ora).default({
    text: "Initialising Fragments config",
    spinner: "simpleDotsScrolling",
    color: "blue"
}).start();
_fs.default.readFile(_path.default.resolve(__dirname, "./config.js"), "utf8", function(err1, contents) {
    err1 && spinner.fail("Failed to read source config. Remember to run the init command first.");
    _fs.default.writeFile("".concat(process.cwd(), "/fragments.config.js"), contents, function(err) {
        err && spinner.fail("Oops, something went wrong. Failed to write configuration file.");
        spinner.succeed("Configuration file built successfully.");
    });
});
