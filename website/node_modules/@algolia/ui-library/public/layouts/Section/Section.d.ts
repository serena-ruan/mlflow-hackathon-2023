import { FunctionComponent } from 'react';
export declare type BackgroundType = 'black' | 'dark' | 'gradient' | 'gradientCenter' | 'gradientSide' | 'light-grey' | 'transparent' | 'white';
export interface SectionProps {
    /** The background of the component */
    background?: BackgroundType;
    /** Removes the padding-bottom of the component */
    noPaddingBottom?: boolean;
    /** Removes the padding-top of the component */
    noPaddingTop?: boolean;
    /** Reverse background colors of the split Section. Also set the splitSection to true */
    reverseSection?: boolean;
    /** Split section with two background colors. Split section into 2 sections with dark/light backgrounds on mobile */
    splitSection?: boolean;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** A `Section` is typically used when a new block of content starts at the page level. It usually comes with a `SectionHeader`. */
declare const Section: FunctionComponent<SectionProps>;
export default Section;
