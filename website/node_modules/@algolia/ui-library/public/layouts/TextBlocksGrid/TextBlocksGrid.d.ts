/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
export interface TextWrapperProps {
    item: JSX.Element;
    numbered: boolean;
}
export interface TextBlocksGridProps {
    /** The number of columns per row */
    columnsPerRow?: 2 | 3;
    /** Color of the gap line if hasVisibleGrid is set to true */
    gridColor?: 'white' | 'grey';
    /** Background color of the grid if hasVisibleGrid is set to true */
    gridBackground?: 'white' | 'light-grey' | 'dark' | 'black';
    /** Boolean to display the grid pattern */
    hasVisibleGrid?: boolean;
    /** Transforms the grid into a numbered list */
    numbered?: boolean;
    [key: string]: any;
}
declare const TextBlocksGrid: FunctionComponent<TextBlocksGridProps>;
export default TextBlocksGrid;
