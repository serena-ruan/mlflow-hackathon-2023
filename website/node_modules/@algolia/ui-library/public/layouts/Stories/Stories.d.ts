import { FunctionComponent } from 'react';
export interface TagWrapperProps {
    item: JSX.Element;
    withLargerText?: boolean;
    reverse?: boolean;
    alternate?: boolean;
    index: number;
}
export interface StoriesInterface {
    /** Switches from alternate story blocks to straight story blocks */
    alternate?: boolean;
    /** Reverses the order of the story blocks */
    reverse?: boolean;
    /** Allows more room for text */
    withLargerText?: boolean;
}
/** Components that are used to wrap several `Story` components together. */
declare const Stories: FunctionComponent<StoriesInterface>;
export default Stories;
