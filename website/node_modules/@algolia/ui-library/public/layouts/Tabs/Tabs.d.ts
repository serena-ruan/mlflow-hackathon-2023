import React from 'react';
import { LabelTextProps } from '../../components/LabelText/LabelText';
export interface TabsProps {
    /** Display a box shadow on selected Tab element */
    boxShadow?: 'none' | 'normal';
    /** An object containing content wrapper properties */
    contentProps?: {
        [key: string]: any;
    };
    /** Data for rendering tab contents */
    data: Array<{
        label: string;
        color?: 'blue' | 'green' | 'orange' | 'pink' | 'red';
        children?: React.ReactChild;
    }>;
    /** Define a default tab based on label */
    defaultTab?: string;
    /** An object containing tab properties */
    tabProps?: LabelTextProps;
    [key: string]: any;
}
declare const Tabs: ({ boxShadow, contentProps, data, defaultTab, tabProps, ...other }: TabsProps) => JSX.Element;
export default Tabs;
