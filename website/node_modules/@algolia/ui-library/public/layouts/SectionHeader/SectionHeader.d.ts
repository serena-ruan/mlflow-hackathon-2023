/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
import { Heading2Props } from '../../components/Heading2/Heading2';
import { LabelTextProps } from '../../components/LabelText/LabelText';
export declare const TEXT_ALIGNS: {
    center: string;
    left: string;
    right: string;
};
export interface TagWrapperProps {
    item: JSX.Element;
}
export declare type alignType = 'left' | 'center' | 'right';
export interface SectionHeaderProps {
    /** The alignment of the title */
    align?: alignType;
    /** The text of the label above the title */
    label?: string | React.ReactNode;
    /** An object containing LabelText properties. See LabelText for more details */
    labelProps?: LabelTextProps;
    /** Removes the margin-bottom of the component */
    noMarginBottom?: boolean;
    /** The title of the component (Heading2) */
    title: React.ReactNode;
    /** An object containing Heading2 properties. See Heading2 for more details */
    titleProps?: Heading2Props;
    [key: string]: any;
}
/**  This is the headline to be used on each `Section` that requires an introduction.
 *
 *  You can use this component to display a `Heading2`, or a `Heading2` plus some introduction text. Use it only once per `Section`.
 */
declare const SectionHeader: FunctionComponent<SectionHeaderProps>;
export default SectionHeader;
