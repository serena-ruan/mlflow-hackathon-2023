/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
import { LightCtaProps } from '../../components/LightCta/LightCta';
export interface LightBannerProps {
    /** Text of the LightCta component */
    cta?: string;
    /** Props of the LightCta component. See LightCta for more details */
    ctaProps?: LightCtaProps;
    /** <i>DEPRECATED</i> Use the cta property instead.
     *
     * Text of the LightCta component.
     */
    ctaText?: string;
    /** Adds an Icon or a Feather icon to the component, as a React component itself */
    icon: React.ElementType | React.FunctionComponent;
    /** An object containing icon properties */
    iconProps?: {
        [key: string]: any;
    };
    [key: string]: any;
}
/** The `LightBanner`, as the name would suggest, is a mix between a simple banner and a <a href="/docs/navigation-lightcta--basic">`LightCta`</a> component.
 *
 * Primarely used as a redirection banner. See <a href="/docs/navigation-lightcta--basic">`LightCta`</a> for more information on ctaProps.
 * */
declare const LightBanner: FunctionComponent<LightBannerProps>;
export default LightBanner;
