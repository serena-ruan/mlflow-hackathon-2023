import { FC } from 'react';
export interface CarouselProps {
    /** Automatically swap cards every X (timing property) seconds */
    auto?: boolean;
    /** Theme of the component */
    theme?: 'dark' | 'light';
    /** Time between 2 cards swap, in millisecondes */
    timing?: number;
    /** Longer size of the carousel */
    isWide?: boolean;
    [key: string]: any;
}
declare const Carousel: FC<CarouselProps>;
export default Carousel;
