import { SerializedStyles } from '@emotion/react';
declare type DefinePositionStyle = (args: {
    currentSlide: number;
    slideIndex: number;
    slideLength: number;
    isWide: boolean;
}) => (SerializedStyles | string | null)[];
export declare const definePositionStyle: DefinePositionStyle;
export {};
