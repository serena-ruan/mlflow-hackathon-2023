"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.definePositionStyle = void 0;
const Carousel_css_1 = __importDefault(require("./Carousel.css"));
const getPositionByIndex = ({ currentSlide, slideIndex, slideLength, }) => {
    if (currentSlide === slideIndex + 1 ||
        currentSlide === slideIndex - (slideLength - 1))
        return -1;
    if (currentSlide === slideIndex)
        return 0;
    if (currentSlide === slideIndex - 1 ||
        currentSlide === slideIndex + (slideLength - 1))
        return 1;
    return 2;
};
exports.definePositionStyle = ({ currentSlide, slideIndex, slideLength, isWide, }) => {
    const getPosition = getPositionByIndex({
        currentSlide: slideIndex,
        slideIndex: currentSlide,
        slideLength,
    });
    if (getPosition === -1)
        return ['uil-d-block', Carousel_css_1.default.getRightSlide(isWide)];
    if (getPosition === 0)
        return ['', Carousel_css_1.default.middleSlide];
    if (getPosition === 1)
        return ['uil-d-block', Carousel_css_1.default.getLeftSlide(isWide)];
    return ['md:uil-d-none', null];
};
//# sourceMappingURL=utils.js.map