/// <reference types="react" />
/** @jsxRuntime classic /
/** @jsx jsx */
import { jsx } from '@emotion/react';
export interface UserGeneratedContentProps {
    /** The content of the component */
    content: string;
    /** Indicates format of the content */
    format?: 'html' | 'markdown';
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** `UserGeneratedContent` is a component that allows the user to add any kind of content inside, from pure text to HTML markup.
 * This component is mostly used to render data fetched from [Contentful](https://contentful.com) or other datasources. We transform the markdown to HTML that is injected into the page.
 */
declare const UserGeneratedContent: {
    ({ content, tag: CustomTag, format, ...other }: UserGeneratedContentProps): jsx.JSX.Element;
    displayName: string;
};
export default UserGeneratedContent;
