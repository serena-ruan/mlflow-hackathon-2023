"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDemoButtonLink = exports.getDefaultHref = void 0;
const isAnchorTag = (internalLinkTag) => internalLinkTag === 'a';
exports.getDefaultHref = (route, withUrlPrefix) => (withUrlPrefix ? `https://www.algolia.com${route}` : route);
exports.useDemoButtonLink = ({ defaultHref, internalLinkTag, trackEvent, ctaProps, }) => {
    const onClick = (event) => {
        if (!ctaProps && trackEvent) {
            trackEvent('Clicked Request Demo', {
                source: 'algolia_webstatic.footer',
            });
        }
        if (ctaProps && ctaProps.onClick) {
            ctaProps.onClick(event);
        }
    };
    const link = ctaProps && ctaProps.href ? ctaProps.href : defaultHref;
    return {
        onClick,
        tag: internalLinkTag || 'a',
        [isAnchorTag(internalLinkTag) ? 'href' : 'to']: link,
        background: 'white',
        color: 'grey',
    };
};
//# sourceMappingURL=utils.js.map