/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
export interface Heading3Props {
    /** The color of the component */
    color?: 'grey' | 'white';
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** This component is used in most cases inside a `SectionHeader` or as the name would imply, as a `Section` title.
 *
 * There are few other occasions where it can also be used as stand-alone element.
 */
declare const Heading3: FunctionComponent<Heading3Props>;
export default Heading3;
