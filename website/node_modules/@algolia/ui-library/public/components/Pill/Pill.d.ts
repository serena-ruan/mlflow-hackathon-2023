/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
export interface PillProps {
    /** Changes the style of the component to inactive */
    active?: boolean;
    /** The color of the active component */
    color?: 'blue' | 'green' | 'orange' | 'pink';
    /** Change the cursor type on hover */
    cursor?: 'auto' | 'pointer' | 'not-allowed';
    /** Toggle component border radius size */
    rounded?: boolean;
    /** Changes the size of the component */
    small?: boolean;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** As an example, you can use a `Pill` if you want to show filterable options. */
declare const Pill: FunctionComponent<PillProps>;
export default Pill;
