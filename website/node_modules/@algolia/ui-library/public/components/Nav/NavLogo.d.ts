import { jsx } from '@emotion/react';
interface NavLogoProps {
    theme?: 'light' | 'dark';
    logo?: {
        src: string;
        alt: string;
    };
    onLogoContextMenu: Function;
}
declare const NavLogo: ({ logo, theme, onLogoContextMenu }: NavLogoProps) => jsx.JSX.Element;
export default NavLogo;
