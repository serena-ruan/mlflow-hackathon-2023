import { FunctionComponent, ElementType } from 'react';
import { RecommendedLinkProps } from './RecommendedLinks/RecommendedLink';
export interface FooterProps {
    /** An object to replace the "Get a demo" CTA */
    ctaProps?: {
        onClick?: Function;
        children?: string;
        href?: string;
    };
    /** A list of all the routes that aren't handled by internal routing */
    externalRoutes?: string[];
    /** Specifies if the footer should contain the CTA banner. Defaults to true*/
    hasBanner?: boolean;
    /** A boolean to display the "Get a demo" CTA */
    includeCta?: boolean;
    /** Specifies the internal tag to be used for routing, typically a `Link` */
    internalLinkTag?: ElementType | FunctionComponent;
    /** An object containing an array of links with a url and a title (example: `{title: "Recommended content", content: [{title: 'Title of a link', url: 'www.link.com'}]}`)*/
    recommendedLinks?: {
        title?: string;
        content?: RecommendedLinkProps[];
    };
    /** Specifies the type of the footer (product or basic). */
    type?: 'default' | 'search' | 'answers' | 'recommend';
    /** An object whose properties, in the shape of an array, should match the title of the link to track. Those properties will be used as function arguments for `trackEvent` */
    trackedEvents?: {
        [key: string]: any;
    };
    /** The function to use in order to allow tracking */
    trackEvent?: Function;
    /** Specificies if all URLs should be prefixed by https://www.algolia.com */
    withUrlPrefix?: boolean;
    /** Other props */
    [key: string]: any;
}
declare const Footer: FunctionComponent<FooterProps>;
export default Footer;
