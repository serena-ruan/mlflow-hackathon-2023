import { jsx } from '@emotion/react';
import Default from './Default';
import Product from './Product';
const Banner = ({ type, internalLinkTag, externalRoutes, ctaProps, trackEvent, withUrlPrefix, }) => {
    if (type === 'default') {
        return (jsx(Default, { internalLinkTag: internalLinkTag, ctaProps: ctaProps, trackEvent: trackEvent, withUrlPrefix: withUrlPrefix }));
    }
    return (jsx(Product, { type: type, internalLinkTag: internalLinkTag, ctaProps: ctaProps, trackEvent: trackEvent, externalRoutes: externalRoutes, withUrlPrefix: withUrlPrefix }));
};
export default Banner;
