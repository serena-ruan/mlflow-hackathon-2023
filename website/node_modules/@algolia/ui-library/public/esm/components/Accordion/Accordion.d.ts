/// <reference types="@emotion/core" />
/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
/** It can be used as a standalone component e.g. Question/Answer component in a FAQ container. */
export interface AccordionProps {
    /** Adds a button to the component */
    button: React.ReactNode;
    /** Should the component be open by default */
    defaultOpen?: boolean;
    [key: string]: any;
}
declare const Accordion: FunctionComponent<AccordionProps>;
export default Accordion;
