/** @jsxRuntime classic /
/** @jsx jsx */
import { jsx } from '@emotion/react';
import { Suspense, useRef, } from 'react';
import useIntersectionObserver from './hook';
const LazyLoader = ({ children, fallback, margin }) => {
    const ref = useRef(null);
    const isComponentVisible = useIntersectionObserver(ref, margin);
    return (jsx("div", { ref: ref }, isComponentVisible ? (jsx(Suspense, { fallback: fallback }, children)) : null));
};
export default LazyLoader;
