import { FC, ReactChild, ReactFragment, ReactPortal } from 'react';
export interface LazyLoaderProps {
    /** A React Node that will be displayed during the loading of the children. */
    fallback: boolean | ReactChild | ReactFragment | ReactPortal | null;
    /** Margin that'll be used to trigger intersection (default is 0). Can be in % or in px, supports CSS syntax.  */
    margin?: string;
}
declare const LazyLoader: FC<LazyLoaderProps>;
export default LazyLoader;
