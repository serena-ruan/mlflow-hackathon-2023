import cx from 'classnames';
import { jsx } from '@emotion/react';
const COLORS = {
    blue: 'uil-color-nebula-500',
    green: 'uil-color-green-700',
    grey: 'uil-color-grey-200',
    'light-grey': 'uil-color-grey-100',
    pink: 'uil-color-pink-600',
    white: 'uil-color-white',
};
/** The `InlineLink` component is for adding links in text. Doing this ensures consistent styling.
 *
 * Use the component inside a block of <a href="/docs/texts-text--basic">`Text`</a> to create a link.
 */
const InlineLink = ({ children, color = 'blue', tag: CustomTag = 'a', ...other }) => {
    const { className: otherClassName, ...otherProps } = other;
    return (jsx(CustomTag, Object.assign({}, otherProps, { className: cx(`uil-td-none hover:uil-td-none`, COLORS[color], otherClassName) }), children));
};
export default InlineLink;
