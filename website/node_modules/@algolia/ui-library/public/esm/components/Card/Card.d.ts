/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
declare type PaddingType = 'none' | 'small' | 'normal' | 'large';
export interface CardProps {
    /** Adds a badge on the top of the component */
    badge?: string;
    /** Defines the color of the background */
    background?: 'light' | 'dark' | 'blue' | 'green';
    /** The border-radius of the component */
    borderRadius?: 0 | 2 | 4 | 6 | 8 | 20;
    /** The box-shadow of the component */
    boxShadow?: 'none' | 'normal' | 'large';
    /** Adds an image to the component, when used within a FeaturesCarousel */
    carouselImage?: string;
    /** Adds a hover effect. Use this when the Card is a link */
    elevation?: boolean;
    /** Adds an image to the component */
    image?: string;
    /** An object containing image properties */
    imageProps?: React.ImgHTMLAttributes<HTMLImageElement>;
    /** The padding of the component */
    padding?: PaddingType;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    /** An object containing video properties */
    videoProps?: React.VideoHTMLAttributes<HTMLVideoElement>;
    [key: string]: any;
}
/**
 * It should be used as a standalone component, recommended inside a <a href="/docs/layouts-section--basic">`Section`</a>.
 *
 * They should be easy to scan for relevant and actionable information. Images can be used to provide more context to the text.
 */
declare const Card: FunctionComponent<CardProps>;
export default Card;
