import { css } from '@emotion/react';
import * as breakpoints from '../../constants/breakpoints';
const styles = {
    codeSampleSection: css `
    width: fit-content;

    @media (min-width: ${breakpoints.MD}) {
      height: 520px;
    }
  `,
};
export default styles;
