/** @jsxRuntime classic /
/** @jsx jsx */
import { jsx } from '@emotion/react';
import { useState } from 'react';
import cx from 'classnames';
import languagesData from './data';
import CodeHighlighter from './CodeHighlighter';
import LanguagesList from './LanguagesList';
import styles from './CodeSample.css';
const CodeSample = ({ title, color, languages, defaultLanguage, className, css, }) => {
    const [currentLanguageKey, setCurrentLanguageKey] = useState(defaultLanguage || Object.keys(languages)[0]);
    const currentLanguage = languagesData[currentLanguageKey];
    return (jsx("div", { css: css
            ? { ...styles.codeSampleSection, ...css }
            : { ...styles.codeSampleSection }, className: cx('uil-d-flex uil-fxd-column md:uil-fxd-row', className) },
        jsx(LanguagesList, { title: title, color: color, languages: languages, currentLanguageKey: currentLanguageKey, setCurrentLanguageKey: setCurrentLanguageKey }),
        jsx(CodeHighlighter, { language: currentLanguageKey, name: currentLanguage.name, ctaLink: currentLanguage.link })));
};
export default CodeSample;
