/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
interface BigIconLink {
    /** The color of the component */
    color?: 'blue' | 'green' | 'grey' | 'pink' | 'red' | 'white';
    /** The Icon to display */
    icon: React.ElementType | React.FunctionComponent;
    /** Props for the Icon, like: 'src', 'alt' and 'isUiLibrary'*/
    iconProps?: {
        [key: string]: any;
    };
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** `BigIconLink` is a type of link component. It's got unique styling with an arrow icon and a leading Bigger icon.
 * You can specify the icon to use with the icon prop.

 * The `BigIconLink` is primarily used in a grid display where you'd want multiples columns.
 * It uses a 100% width flex display. A container must be used to restrict the width.
 */
declare const BigIconLink: FunctionComponent<BigIconLink>;
export default BigIconLink;
