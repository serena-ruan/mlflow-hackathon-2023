/** @jsxRuntime classic /
/** @jsx jsx */
import { ReactNode } from 'react';
import { jsx } from '@emotion/react';
export interface SharedProps {
    /** The icon of the list items */
    icon?: ReactNode;
    /** The props of the icon */
    iconProps?: {
        /** The color of the icon */
        color?: 'blue' | 'white';
        /** The className of the icon */
        className?: string;
        /** The css of the icon */
        css?: object;
    };
    color?: 'grey' | 'white';
    small?: boolean;
}
export interface ListProps extends SharedProps {
    [key: string]: any;
}
export interface TagWrapperProps extends SharedProps {
    /** The list item */
    item: JSX.Element;
}
/**
 * `List` is a component that aims to bring a basic design to lists
 *
 * Use `List` when you need to generate a list with or without icons before the items
 */
declare const List: ({ children, color, iconProps, icon, ...other }: ListProps) => jsx.JSX.Element;
export default List;
