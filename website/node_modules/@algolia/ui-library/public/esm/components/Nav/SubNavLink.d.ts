/// <reference types="react" />
import { jsx } from '@emotion/react';
import { LinkType } from './SubNav';
interface SubNavLinkProps {
    index: number;
    isSidebar?: boolean;
    link: LinkType;
    section: string;
    tag: React.ElementType;
    withSmallIcon?: boolean;
    [key: string]: any;
}
declare const SubNavLink: ({ index, isSidebar, link, section, tag, withSmallIcon, ...other }: SubNavLinkProps) => jsx.JSX.Element;
export default SubNavLink;
