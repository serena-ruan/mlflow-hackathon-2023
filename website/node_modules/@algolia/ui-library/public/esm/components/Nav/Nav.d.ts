/// <reference types="react" />
/** @jsxRuntime classic /
/** @jsx jsx */
import { jsx } from '@emotion/react';
import { SecondaryNavProps } from './SecondaryNav';
export interface NavProps {
    /** A list of all the routes that aren't handled by internal routing */
    externalRoutes?: string[];
    /** Specifies the internal tag to be used for routing, typically a `Link` */
    internalLinkTag?: React.ElementType | React.FunctionComponent;
    /** Specifies if a user is logged in */
    isLoggedIn?: boolean;
    /** Use an other logo than the algolia default logo */
    logo?: {
        src: string;
        alt: string;
    };
    /** The nav displays a secondary nav */
    secondaryNav?: SecondaryNavProps;
    /** The theme of the component */
    theme?: 'dark' | 'light';
    /** An object whose properties, in the shape of an array, should match the title of the link to track. Those properties will be used as function arguments for `trackEvent` */
    trackedEvents?: {
        [key: string]: any;
    };
    /** The function to use in order to allow tracking */
    trackEvent?: Function;
    /** Enable the search bar to be translucid. */
    translucidSearch?: boolean;
    /** Specificies if all URLs should be prefixed by https://www.algolia.com */
    withUrlPrefix?: boolean;
}
/**
 *
 * `Nav` is the main navigation currently used on algolia.com.
 *
 * # Context
 *
 * It is designed to be framework-agnostic, and can be customized to define internal and external routes.
 *
 */
declare const Nav: ({ externalRoutes, internalLinkTag, isLoggedIn, logo, secondaryNav, theme, trackedEvents, trackEvent, translucidSearch, withUrlPrefix, }: NavProps) => jsx.JSX.Element;
export default Nav;
