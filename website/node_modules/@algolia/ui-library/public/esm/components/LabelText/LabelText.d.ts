/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
export interface LabelTextProps {
    /** Make the text bigger */
    big?: boolean;
    /** The color of the component */
    color?: 'blue' | 'green' | 'grey' | 'orange' | 'pink' | 'red' | 'white';
    /** The font of the component */
    font?: 'jetbrains' | 'poppins';
    /** Adds an Icon or a Feather icon to the component, as a React component itself */
    icon?: React.ElementType | React.FunctionComponent;
    /** An object containing icon properties */
    iconProps?: {
        [key: string]: any;
    };
    /** Adds an (optional) after the text */
    optional?: boolean;
    /** Adds an asterisk after the text */
    required?: boolean;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** The `LabelText`, as the name would suggest, is primarily used as a label for forms, but also for shorter texts, mostly as additional information like category or type.
 *
 * It is important to note that the tag defaults to a `span`, not `label`.
 * */
declare const LabelText: FunctionComponent<LabelTextProps>;
export default LabelText;
