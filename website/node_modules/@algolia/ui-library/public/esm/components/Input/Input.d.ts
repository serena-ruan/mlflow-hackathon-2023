/// <reference types="react" />
import { jsx } from '@emotion/react';
export interface InputProps {
    /** Defines if the component is disabled or not */
    disabled?: boolean;
    /** The placeholder of the component */
    placeholder?: string;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    /** The type of the input */
    type?: string;
    /** The value of the input */
    value?: string;
    [key: string]: any;
}
/**
 * `Input` is a generic component to handle inputs. It can have different types.
 *
 * Use `Input` in place of standard inputs and textarea fields.
 */
declare const Input: ({ disabled, placeholder, tag: CustomTag, type, value, ...other }: InputProps) => jsx.JSX.Element;
export default Input;
