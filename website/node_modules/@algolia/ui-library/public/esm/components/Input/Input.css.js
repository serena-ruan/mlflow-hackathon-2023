import { css } from '@emotion/react';
import * as colors from '../../constants/colors';
export default {
    root: css `
    &:active,
    &:focus {
      border-color: ${colors.NEBULA_500};
    }
    &::placeholder {
      color: ${colors.GREY_400};
    }
  `,
};
