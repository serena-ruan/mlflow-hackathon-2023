/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
export interface ModalProps {
    /** Toggle visibility of the close icon */
    displayCloseIcon?: boolean;
    /** The function that closes the component */
    handleClose: (event?: React.MouseEvent | KeyboardEvent) => void;
    /** Adapts the styles of the Modal for a video */
    hasVideo?: boolean;
    /** Toggle visibility of the component */
    isActive?: boolean;
    /** Increases the width of the component */
    large?: boolean;
    /** The target element which to render the Modal within */
    targetEl?: Element;
    /** The theme of the overlay */
    theme?: 'dark' | 'light';
    [key: string]: any;
}
/** The `Modal` component is useful for creating dialogs for various types of forms, such as registration or signup forms. */
declare const Modal: FunctionComponent<ModalProps>;
export default Modal;
