import { jsx } from '@emotion/react';
export interface SwitchProps {
    /** Checkbox properties used to customize the input type=checkbox element.
     * It allows all HTMLInputElement properties, except 'type' one.
     * */
    inputProps?: {
        checked?: boolean;
    } & Omit<React.HTMLAttributes<HTMLInputElement>, 'type'>;
}
/**
 * `Switch` is a component to handle toggle actions. It is using an input `type='checkbox'`.
 * This component can be activated ("toggled") programmatically using the `checked` property, inside `inputProps`.
 */
declare const Switch: ({ inputProps, ...other }: SwitchProps & Record<string, any>) => jsx.JSX.Element;
export default Switch;
