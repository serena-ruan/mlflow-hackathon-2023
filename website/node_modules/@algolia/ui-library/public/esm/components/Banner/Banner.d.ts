/// <reference types="react" />
import { jsx } from '@emotion/react';
import { ButtonProps } from '../Button/Button';
export interface BannerProps {
    /** Adds a Button to the component */
    button: React.ReactNode;
    /** An object containing Button properties. See Button for more details */
    buttonProps?: ButtonProps;
    /** <i>(URL)</i> Adds an image to the component */
    image: string;
    /** An object containing image properties */
    imageProps: React.ImgHTMLAttributes<HTMLImageElement>;
    /** Make the button primary */
    primary?: boolean;
    /** Subtitle of the banner */
    subtitle?: string;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    /** The theme of the component */
    theme?: 'orange' | 'blue';
    /** Title of the banner */
    title: React.ReactNode;
    /** Specificies if all URLs should be prefixed by https://www.algolia.com */
    withUrlPrefix?: boolean;
    [key: string]: any;
}
/** It can be used as a standalone component e.g. a promotion offer, a study you want to put forward. */
declare const Banner: ({ button, buttonProps, image, imageProps, primary, subtitle, tag: CustomTag, theme, title, withUrlPrefix, ...other }: BannerProps) => jsx.JSX.Element;
export default Banner;
