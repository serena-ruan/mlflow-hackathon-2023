import { jsx } from '@emotion/react';
export interface TableProps {
    /** The data of the table */
    data?: Array<Array<string | number>>;
    /** The heading cells of the table */
    headCells?: Array<string | number>;
    /** Determines if the first column will be used for row labels */
    withLabels?: boolean;
    /** Determines if the table will be sticky */
    withStickyHead?: boolean;
    [key: string]: any;
}
declare const Table: ({ data, headCells, withLabels, withStickyHead, ...other }: TableProps) => jsx.JSX.Element;
export default Table;
