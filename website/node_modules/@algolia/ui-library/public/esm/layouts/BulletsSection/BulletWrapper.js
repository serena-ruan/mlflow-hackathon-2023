import cx from 'classnames';
import { jsx } from '@emotion/react';
const BulletWrapper = ({ item }) => {
    if (!item) {
        return null;
    }
    const CustomTag = item.type;
    const isCardItem = CustomTag.displayName === 'Card';
    return (jsx(CustomTag, Object.assign({}, item.props, { className: cx(item.props.className, 'uil-w-100p uil-h-100p', isCardItem
            ? 'uil-gcstart-1 uil-gcend-3 uil-mb-16 sm:uil-mb-20'
            : 'uil-mb-24 sm:uil-mb-0') })));
};
BulletWrapper.displayName = 'BulletWrapper';
export default BulletWrapper;
