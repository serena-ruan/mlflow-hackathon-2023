/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
import { XOR } from '../../constants/types';
export interface TagWrapper {
    item: JSX.Element;
}
/** `Story` is a layout component, the component is supposed to show a set of informations on pages. They usually are used to present features.
 *
 * Each story is part of a parent component called `Stories`. Mostly used on pages to present information.
 */
interface StoryBasicProps {
    /** Set the position of the text */
    position?: 'start' | 'center' | 'end';
    /** Reverses image and text */
    reverse?: boolean;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    /** Allows more room for text */
    withLargerText?: boolean;
    [key: string]: any;
}
interface StoryWithVideo {
    /** Adds a video next to the story */
    video: string;
    /** An object containing video properties */
    videoProps?: React.VideoHTMLAttributes<HTMLVideoElement>;
}
interface StoryWithImage {
    /** Adds a image next to the story */
    image: string;
    /** An object containing image properties */
    imageProps?: React.ImgHTMLAttributes<HTMLImageElement>;
}
export declare type StoryProps = StoryBasicProps & XOR<StoryWithVideo, StoryWithImage>;
declare const Story: FunctionComponent<StoryProps>;
export default Story;
