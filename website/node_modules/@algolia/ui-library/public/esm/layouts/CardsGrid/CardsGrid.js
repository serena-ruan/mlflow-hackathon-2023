import React, { Children } from 'react';
import cx from 'classnames';
import { ErrorBoundary } from '../../index';
const GRID_COLUMNS = {
    2: 'md:uil-g-2',
    3: 'md:uil-g-3',
    4: 'md:uil-g-4',
    5: 'md:uil-g-5',
    6: 'md:uil-g-6',
};
const CardWrapper = ({ item }) => {
    if (!item)
        return null;
    if (item.type && item.type.displayName === 'Card') {
        const CustomTag = item.type;
        return (React.createElement("li", { className: "uil-lis-none" },
            React.createElement(CustomTag, Object.assign({}, item.props, { className: cx('uil-h-100p', item.props.className) }))));
    }
    throw new Error("You can only use 'Card' component.");
};
const CardsGrid = ({ children, columnsPerRow = 3, ...other }) => (React.createElement("div", Object.assign({}, other),
    React.createElement("ul", { className: cx('uil-d-grid uil-ggap-24 uil-m-0 uil-p-0', GRID_COLUMNS[columnsPerRow], columnsPerRow <= 3 && 'lg:uil-ggap-48', columnsPerRow === 4 && 'lg:uil-ggap-32', columnsPerRow >= 5 && 'lg:uil-ggap-24') }, Children.map(children, item => (React.createElement(ErrorBoundary, null,
        React.createElement(CardWrapper, { item: item })))))));
export default CardsGrid;
