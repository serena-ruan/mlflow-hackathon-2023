/// <reference types="react" />
import { jsx } from '@emotion/react';
export interface DataType {
    icon: React.ElementType | React.FunctionComponent;
    iconProps?: {
        [key: string]: any;
    };
    title: string;
    subtitle: string;
}
export interface FeaturesRowWrapperProps {
    data: DataType[];
    padding: string;
    [key: string]: any;
}
export interface FeaturesRowProps {
    /** Defines the color of the background */
    background?: 'light' | 'dark';
    /** Data to loop over to render the component */
    data: Array<{
        icon: React.ElementType | React.FunctionComponent;
        iconProps?: {
            [key: string]: any;
        };
        title: string;
        subtitle: string;
    }>;
    /** The padding of the component */
    padding?: string;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    [key: string]: any;
}
/** `FeaturesRow` is used to highlight top features or characteristics of a product.
 *
 * Preferably used within a `Section`, below a `Heading2` and `Text`, to give more context to this component.
 */
declare const FeaturesRow: {
    ({ background, data, padding, tag: CustomTag, ...other }: FeaturesRowProps): jsx.JSX.Element;
    displayName: string;
};
export default FeaturesRow;
