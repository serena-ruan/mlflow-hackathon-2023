/** @jsxRuntime classic /
/** @jsx jsx */
import { FunctionComponent } from 'react';
import { Heading3Props } from '../../components/Heading3/Heading3';
export interface TagWrapper {
    item: JSX.Element;
    index: number;
    isIconOutside: boolean;
}
export interface TextBlockProps {
    /** The color of the label and the link */
    color?: 'blue' | 'green' | 'grey' | 'orange' | 'pink' | 'red' | 'white';
    /** Adds an Icon or a Feather icon to the component, as a React component itself */
    icon?: React.ElementType | React.FunctionComponent;
    /** The position of the Icon */
    iconPosition?: 'label' | 'title' | 'above' | 'outside';
    /** An object containing icon properties */
    iconProps?: {
        [key: string]: any;
    };
    /** The text of the label above the title */
    label?: string | React.ReactNode;
    /** The HTML tag of the component */
    tag?: React.ElementType | React.FunctionComponent;
    /** The title of the component (Heading3) */
    title?: React.ReactNode;
    /** An object containing Heading3 properties. Check Heading3 component for more information */
    titleProps?: Heading3Props;
    /** Makes the title font-size bigger */
    withBigTitle?: boolean;
    [key: string]: any;
}
/** `TextBlock` component is using different components like `Text` or `LightCta`.
 * It's made to be a block of text, with a title. Correct spacing is automatically handled around the elements within the component.
 *
 * It can be used in a parent component, on `TextsRow`.
 */
declare const TextBlock: FunctionComponent<TextBlockProps>;
export default TextBlock;
